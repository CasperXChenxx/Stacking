This repository contains **R code** for experiments on **stacking** (and related model-selection baselines), including:
- **Simulation studies** (see `Simulation codes/`)
- **Real-data experiments** (see `Real data/`)
